require 'rails_helper'
require 'spec_helper'
RSpec.describe DisplayordersController, type: :controller do
     describe "Display orders" do
    it "Display orders new page" do
   #   expect(Displayorder).to receive(:display)
    #  expect("customerhire").to receive(:new)
      #get :display
    #  expect(response).to receive()
    end
  end
end
